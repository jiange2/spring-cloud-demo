---
eureka:
  client:
    service-url:
      defaultZone: http://localhost:9001/eureka/
spring:
  application:
    name: hello-service
  profiles: peer1
server:
  port: 10011
---
eureka:
  client:
    service-url:
      defaultZone: http://localhost:9001/eureka/
spring:
  application:
    name: hello-service
  profiles: peer2
server:
  port: 10012
---
eureka:
  client:
    service-url:
      defaultZone: http://localhost:9001/eureka/
spring:
  application:
    name: hello-service
  profiles: peer3
server:
  port: 10013
---
eureka:
  client:
    service-url:
      defaultZone: http://localhost:9001/eureka/
spring:
  application:
    name: hello-service
  profiles: peer4
server:
  port: 10014
---